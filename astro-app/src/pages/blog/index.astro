---
import BlogPost from '../../layouts/BlogPost.astro';

let posts = await Astro.glob('../../content/articles/*.md');
let post = posts[1];
const headings = post.getHeadings();

---
<BlogPost title={headings[0].text} headings={headings}>
    <post.Content/>
</BlogPost>